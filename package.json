{
  "name": "form-project",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "serve": "npx serve -s . -p 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "serve": "^14.2.0"
  }
}